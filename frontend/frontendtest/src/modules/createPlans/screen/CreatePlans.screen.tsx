import React from 'react';
import { SidebarComponent } from '../../../core/components/Sidebar';
import Form from '../components/FormCreate';

export const CreatePlansScreen = () => {
    return (
        <div>
            <Form />
        </div>
    );
}